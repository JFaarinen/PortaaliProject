import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    mainContainer: {
        display: 'flex',
        alignItems: 'center',
    },

    container: {
        margin: 'auto',
        maxWidth: '100%',
        padding: '2rem',
    }

}));